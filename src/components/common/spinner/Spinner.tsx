import React from 'react';

import './Spinner.scss';

const Spinner = () => {
    return (
        <div className="Spinner">
            <div className="Spinner__animation">

            </div>
        </div>
    );
};

export default Spinner;

